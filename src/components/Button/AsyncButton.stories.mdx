import {
  Meta,
  Story,
  Source,
  SourceState,
  Canvas
} from '@storybook/addon-docs/blocks';
import { GoCheck as CheckIcon } from 'react-icons/go';
import * as stories from './AsyncButton.stories.tsx';
import { Box } from '../Box/Box';
import { AsyncButton } from './AsyncButton';

<Meta
  title="Design System/Molecules/Buttons/AsyncButton"
  component={AsyncButton}
/>

# AsyncButton

The `AsyncButton` component can be used to display a loading state. Pass the `isLoading` prop to show its loading state. By default, the button will show a spinner and leave the button's width unchanged.

You can also pass the `loadingText` prop to show a spinner and the loading text.

## Import

<Source
  dark
  language="typescript"
  code="import { AsyncButton } from '@f1v/zati';"
/>

## Usage

<Canvas withSource={SourceState.NONE} mdxSource={false}>
  <Story story={stories.Usage2} name="Usage" />
</Canvas>

###

<Source dark language="jsx" storyId="Usage" />

## Advanced Usage

<Canvas>
  <Story story={stories.AdvancedUsage} name="Advanced Usage" />
</Canvas>
